package com.finotech.jewellery.modules.notification.domain.enums;

public enum NotificationChannel {
    EMAIL,
    SMS,
    PUSH,
    /** Shown inside the ERP to staff rather than sent to a customer. */
    IN_APP
}
